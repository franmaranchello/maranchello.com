rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null &&
        request.auth.uid == "MwMTQwq0D9av2NEyDoTi05w3XRU2";
    }

    match /{document=**} {
      allow read;
      allow write: if isAdmin();
    }
  }
}
